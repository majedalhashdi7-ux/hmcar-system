I will implement the **Cinematic 3D** transformation for the HM CAR website, focusing on a deep dark luxury aesthetic, immersive 3D background, and glassmorphism UI.

### **Phase 1: 3D Environment Setup (The "Cinematic" Core)**

I will create a reusable 3D Scene component using **Three.js & React Three Fiber** to replace/enhance the current backgrounds.

* **Component:** `components/3d/CinematicBackground.tsx`

* **Features:**

  * **Dynamic Grid/Terrain:** A moving neon grid to simulate speed and depth (Synthwave/Future style).

  * **Floating Particles:** "Dust" or "Embers" floating in 3D space (`<Stars />`, `<Sparkles />` from `@react-three/drei`).

  * **Post-Processing:** Bloom effect for that "Neon Glow" look (`@react-three/postprocessing`).

  * **Optimization:** Ensure it runs smoothly without overheating devices.

### **Phase 2: Landing Page Overhaul (`app/page.tsx`)**

I will upgrade the `LandingShowcase` to be a true "Immersive Experience".

* **Hero Section:**

  * Integrate the `CinematicBackground` behind the content.

  * **Glass Cards (Refined):** Enhance the existing cards with 3D tilt effects (`react-tilt` logic via Framer Motion) so they feel like floating glass shards.

  * **Stats Bar:** Ensure the "Digital Counter" look is vibrant and neon-glowing.

* **Animations:**

  * Implement "Staggered Reveal" for all elements (Cards don't just appear; they float in).

  * **Smooth Scroll:** Ensure the transition between the Hero and the rest of the page is seamless.

### **Phase 3: Global Styling & Auth Pages**

* **Global CSS (`globals.css`):**

  * Enforce "Deep Black" (`#000000` or `#020202`) as the base.

  * Refine `glass-panel` utility to have a "Frosty" look with noise texture if possible.

  * Add `neon-text` and `neon-box` utility classes for easy glow effects.

* **Auth Pages (`/login`,** **`/register`):**

  * Enhance the current Video Background with a **"Vignette" and "Scanline" overlay** to make it look like a digital screen.

  * Make the Login Form **fully transparent (Glass)** so the animated background is clearly visible behind the inputs.

  * Add "Start Engine" animation to the Login button (pulse + flash on click).

### **Phase 4: Component Enhancements**

* **Navigation:** Make the Navbar a floating glass pill that disappears/reappears on scroll.

* **Buttons:** Create a `NeonButton` component that has a trailing

