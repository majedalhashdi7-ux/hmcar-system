// [[ARABIC_HEADER]] هذا الملف (client-app/postcss.config.mjs) جزء من مشروع HM CAR ويحتوي تعليقات عربية لضمان الوضوح.

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
