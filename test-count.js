const mongoose = require('mongoose'); require('dotenv').config({ path: '.env.production.vercel' }); const Brand = require('./models/Brand'); const SparePart = require('./models/SparePart'); mongoose.connect(process.env.MONGO_URI).then(async() => { const b = await Brand.findOne({name: '≈дЁндн н'}); if(b) { const count = await SparePart.countDocuments({brand: b._id}); console.log('Parts:', count); } process.exit(0); });
